{% extends "@internal/wrapper.html.twig" %}
{% block content %}
    <p>
        Service Penalties may be incurred if this request is not fixed soon because the Service
        Request <a href="{{ activityURL }}"
                   target="_blank"
        >{{ serviceRequestId }}</a> will breach its FIX SLA shortly.
    </p>
    <p>
        Details
        {{ details | raw }}
    </p>
{% endblock %}